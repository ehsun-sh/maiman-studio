[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "maiman"
version = "0.0.1.dev0"
description = "Open-source simulator for optical communication links and photonic systems"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Ehsan Shahmohammadi" }]
keywords = ["photonics", "optical-communications", "simulation", "fiber-optics", "dsp"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Physics",
]

# NOTE: FFTW is deliberately absent. It is GPL-2.0-or-later; linking it (directly
# or via pyFFTW) would force this project to GPL. numpy/scipy use pocketfft (BSD).
dependencies = ["numpy>=1.26"]

[project.optional-dependencies]
# hatchling and build are here so the packaging test can assemble a real wheel
# without network isolation. It is the only test that can prove the studio page
# reaches an installed copy, which is the whole reason the page lives in the
# package rather than in docs/.
dev = ["pytest>=8", "mypy>=1.11", "ruff>=0.6", "build>=1.2", "hatchling>=1.25"]

[project.scripts]
# The front door. `python -m maiman.server` still works and is what a checkout
# uses; this is what exists after `pip install maiman`, where there is no source
# tree to know the layout of.
maiman = "maiman.cli:main"

[project.urls]
Homepage = "https://github.com/ehsun-sh/maiman-studio"
Documentation = "https://github.com/ehsun-sh/maiman-studio/blob/main/docs/ARCHITECTURE.md"

[tool.hatch.build.targets.wheel]
# Everything under the package ships, including src/maiman/studio/index.html —
# the interface is data, and a wheel without it installs a server that answers
# every API route and 404s the page it exists to serve. Asserted by
# tests/test_packaging.py against an actually-built wheel, because a default
# that is relied on and never checked is a default that changes.
packages = ["src/maiman"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"

[tool.ruff]
line-length = 100
src = ["src", "tests"]
# Prose documents are written for readers, not for the formatter: aligned comments
# in illustrative snippets are deliberate. Ruff owns the code, not the docs.
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.per-file-ignores]
# N803/N806: physics code uses standard symbol names (Ex, Ey, N, T0, L_D) that do
#   not fit PEP 8 casing. Readability against the literature wins over convention.
# RUF012: `inputs`/`outputs` are class-level port declarations that Component.__init__
#   copies onto each instance, so a component with a configurable port count can
#   rebind its own. Annotating them ClassVar would forbid exactly that.
"src/maiman/**" = ["N803", "N806", "RUF012"]
"tests/**" = ["N802", "N803", "N806"]

[tool.mypy]
# No python_version pin: mypy targets whichever interpreter runs it, so the CI
# matrix type-checks against each supported version. Pinning it to the minimum
# while running on a newer interpreter makes mypy reject dependency stubs that
# use syntax from the newer version.
files = ["src", "tests", "examples"]
disallow_untyped_defs = true
disallow_incomplete_defs = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_unreachable = true
no_implicit_optional = true
strict_equality = true
